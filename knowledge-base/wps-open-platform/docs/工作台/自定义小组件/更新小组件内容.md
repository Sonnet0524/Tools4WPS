
# 更新小组件内容

更新指定自定义小组件实例的数据



**标签**：`工作台-自定义小组件` 


## 请求说明
| 字段          | 值                  |
|--------------|---------------------|
| **<div style="white-space: nowrap;">请求地址</div>**  | https://openapi.wps.cn/v7/workbench_blocks/{block_id}/update_content          |
| **<div style="white-space: nowrap;">HTTP 方法</div>** | `POST`    |
| **<div style="white-space: nowrap;">接口描述</div>**  | 更新指定自定义小组件实例的数据 |
| **<div style="white-space: nowrap;">签名方式</div>**  | [KSO-1](https://open.wps.cn/documents/app-integration-dev/wps365/server/api-description/signature-description) |
| **<div style="white-space: nowrap;">限频策略</div>** | 无 |
| **<div style="white-space: nowrap;">权限要求</div>** | <div><div>查询和管理工作台小组件数据(应用授权) `kso.workbench_block.readwrite`</div></div> |


## 请求头 (Headers)
| 属性名 | 类型 | 是否必填 | 描述 | 可选值 |
|-------|------|------|-------|-------|
| <div style="white-space: nowrap;">X-Kso-Id-Type</div> | `string` | 否 | 类型<br>  * **internal** - 内部<br>  * **external** - 外部<br> | `internal`, `external` 



## 路径参数 (Path)
| 属性名 | 类型 | 是否必填 | 描述 | 可选值 |
|-------|------|------|-------|-------|
| <div style="white-space: nowrap;">block_id</div> | `string` | 是 | 小组件实例ID（注意：不是小组件模板ID，同一个小组件模板在不同工作台创建的实例会拥有不同的实例ID） | - 





## 请求体(Body)
**请求体格式:** `application/json`
<OpenapiRenderTable  dataSource='[{"key":"source","name":"source","deprecated":false,"type":"string","required":"否","enum":[],"xEnum":[],"description":"<p>业务来源，由业务方自定义（建议格式：{企业名}_{业务名}），开放平台不对该字段做唯一性校验，将原样透传给目标小组件实例</p>\n","children":[]},{"key":"source_data","name":"source_data","deprecated":false,"type":"string","required":"否","enum":[],"xEnum":[],"description":"<p>业务数据，将原样透传给目标小组件实例，该字段的设计是为了解决多样化的更新需求，建议以下几种使用方式:<br>\n1.直接更新: source_data中直接携带需要更新的小组件目标数据，小组件解析并渲染该数据；<br>\n2.信号触发: source_data仅作为刷新信号，组件接收到后会主动请求业务方后端接口以获取最新数据并渲染；<br>\n3.混合逻辑：业务方也可以根据自身需求，结合以上两种方式实现更复杂的更新策略；</p>\n","children":[]},{"key":"user_ids","name":"user_ids","deprecated":false,"type":"array[string]","required":"否","enum":[],"xEnum":[],"description":"<p>推送用户列表，可选；为空时默认推送给 block_id 所属工作台的当前在线用户</p>\n","children":[]},{"key":"version","name":"version","deprecated":false,"type":"string","required":"是","enum":[],"xEnum":[],"description":"<p>目标小组件所需的最低版本，格式由业务方自定义（如 x.x.x），将原样透传给目标小组件实例</p>\n","children":[]}]' />

## 请求体示例
```json
{
  "source": "string",
  "source_data": "string",
  "user_ids": [
    "string"
  ],
  "version": "string"
}
```

## 响应体(Response)
**HTTP状态码:** `200`<br/>
**响应体格式:** `application/json`
<OpenapiRenderTable hideHeaderKeys='required' dataSource='[{"key":"code","name":"code","deprecated":false,"type":"integer","required":"是","enum":[],"xEnum":[],"description":"-","children":[]},{"key":"msg","name":"msg","deprecated":false,"type":"string","required":"是","enum":[],"xEnum":[],"description":"<p>人可阅读的文本信息，可能会按不同的语言或地区返回不同的文本信息。</p>\n","children":[]}]' />

## 响应体示例
```json
{
  "code": 0,
  "msg": "string"
}
```


